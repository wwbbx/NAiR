# This script is designed to convert all EMU Test Card to Json
# It accepts first parameter as root path of all EMU Test Cards.
# It accepts second parameter as output root directory.
# It will construct Test Card folder structure.
# For DataSheet of each EMU Test Card, this tool will copy them to
# desitnation directory directly.
